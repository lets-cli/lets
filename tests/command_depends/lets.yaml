shell: bash

commands:
  run-with-depends:
    description: Test command depends
    depends: 
      - name: greet
        args: Developer
      - bar
    cmd: |
      echo "Main"

  greet:
    options: |
      Usage: lets greet <name>
    cmd: echo Hello ${LETSOPT_NAME}

  bar:
    cmd: echo Bar

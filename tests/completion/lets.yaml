shell: bash

commands:
  foo:
    description: Print foo
    cmd: echo "Foo"

  bar:
    description: Print bar
    options: |
      Usage: lets bar [--debug] [--env=<env>]
      Options:
        --debug  Run with debug
        --env=<env>, -e  Set env
    cmd: echo "Bar"
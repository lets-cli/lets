shell: bash

commands:
  bar:
    options: |
      Usage: lets bar [--debug] [--config=<config>] [--only=<only>]
    cmd: |
      echo DEBUG=${LETSOPT_DEBUG}
      echo CONFIG=${LETSOPT_CONFIG}
      echo ONLY=${LETSOPT_ONLY}
